{
  "$comment": "STATUS: Working example — valid JSON Schema (draft 2020-12). Fictional event definition.",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "FulfillmentCompleted",
  "type": "object",
  "required": ["eventId", "eventType", "schemaVersion", "correlationId", "occurredAt", "orderId", "shipmentId", "completedAt"],
  "properties": {
    "eventId": { "type": "string", "format": "uuid" },
    "eventType": { "const": "FulfillmentCompleted" },
    "schemaVersion": { "const": "1.0" },
    "correlationId": { "type": "string", "format": "uuid" },
    "occurredAt": { "type": "string", "format": "date-time" },
    "orderId": { "type": "string" },
    "shipmentId": { "type": "string" },
    "completedAt": { "type": "string", "format": "date-time" }
  }
}
