{
  "$comment": "STATUS: Working example — valid JSON Schema (draft 2020-12). Fictional event definition.",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "OrderCreated",
  "type": "object",
  "required": ["eventId", "eventType", "schemaVersion", "correlationId", "occurredAt", "orderId", "customerId", "lineItems"],
  "properties": {
    "eventId": { "type": "string", "format": "uuid", "description": "Unique per event occurrence; used for deduplication." },
    "eventType": { "const": "OrderCreated" },
    "schemaVersion": { "const": "1.0" },
    "correlationId": { "type": "string", "format": "uuid", "description": "Propagated from the originating Order Process API request." },
    "occurredAt": { "type": "string", "format": "date-time" },
    "orderId": { "type": "string" },
    "customerId": { "type": "string" },
    "lineItems": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["sku", "quantity"],
        "properties": {
          "sku": { "type": "string" },
          "quantity": { "type": "integer", "minimum": 1 }
        }
      }
    }
  }
}
